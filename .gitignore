# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
.env*.local

# Common ignores
node_modules
.next/
.DS_Store
.vercel
.env*

# ...but the documented template of required variable NAMES must be committed.
!.env.example

# Build logs and TypeScript incremental cache — never belong in git.
# tsconfig.tsbuildinfo and t.log were committed in 1582d75 by accident.
*.log
tsconfig.tsbuildinfo
live.html

# Design reference recordings — screen captures used to agree the look of the
# home page, not assets the site serves. semrush-like-dashbaord... is Semrush's
# own product UI; committing it would publish a competitor's dashboard from
# searchprex.com and add ~6MB of permanently unused weight to the repo.
public/video/semrush-inspired-ending.mp4
public/video/semrush-like-dashbaord-wanted-on-home-page.mp4
